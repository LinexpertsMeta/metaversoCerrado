{
  "name": "server",
  "version": "0.0.1",
  "description": "node js server",
  "main": "server.js",
  "engines": {
    "node": "14.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.1",
    "socket.io-client": "^4.7.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build-docker": "sh ./Dockerfile.dev"
  }
}
